using UnityEngine;
using System.Collections;

public class ForceRotationUp : MonoBehaviour
{
	// Update is called once per frame
	void LateUpdate()
	{
		transform.rotation = Quaternion.identity;
	}
}
